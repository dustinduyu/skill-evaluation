# 评估报告模板

使用这个结构产出简洁报告。除非用户要求深度附录，否则控制在管理者 7-8 分钟可读完的长度。

## 执行摘要

> 本节先给结论：这个 skill 是否可运行、是否有价值、主要问题是什么、下一步应该怎么改。

先放一张状态总览表，让读者在第一页看到运行框架层和能力链路层的关键检查项是否满足。状态使用 `✅`、`❌`、`待补测`、`部分满足`。

| 层级 | 检查项 | 状态 | 证据 |
| --- | --- | --- | --- |
| 运行框架层 | 可唤起性 / 路由选择 | ✅ / ❌ / 待补测 | router 日志、触发提示词测试 |
| 运行框架层 | 运行成功率 | ✅ / ❌ / 待补测 | 命令、退出码、日志 |
| 运行框架层 | 成本、耗时、错误可观测 | ✅ / ❌ / 待补测 | 评测运行记录、trace、metrics |
| 能力链路层 | 中间产物完整性 | ✅ / ❌ / 待补测 | schema 校验、输出文件 |
| 能力链路层 | 工具调用和状态交接 | ✅ / ❌ / 待补测 | trace、步骤日志 |
| 能力链路层 | 评判结果可复现 | ✅ / ❌ / 待补测 | 数据记录表、评判器输出 |

包含一张 5-8 行的关键结论表：

| 结论 | 证据 | 对 skill 的含义 |
| --- | --- | --- |
| 链路可以稳定跑通 | 真实运行证据 | 可以进入持续评测 |
| 质量有主要风险 | 分歧样本或失败数据 | 优化特定步骤 |
| 可唤起性待测 / 已测 | 唤起证据 | 优化 name / description 或路由 |
| 存在必要性待测 / 已测 | 无 skill 基线证据 | 证明或修正 skill 价值 |

## Skill 场景理解与分层

> 本节说明评测前先理解了什么：skill 用在什么场景、成功标准是什么、边界在哪里、按三层如何拆。

包含：

- 使用场景
- 成功标准
- 需要人工确认的业务口径
- 评测对象
- 核心链路
- skill 职责
- 边界
- 三层表

## 评测目标与指标体系

> 本节回答“到底评什么”。每个目标都要落到可观测指标和计算规则。

包含：

- 评测目标
- 指标表，列为：指标 / 所属层级 / 计算规则 / 本轮结果
- 指标选择原则
- 评测设计确认记录：确认人、确认时间、确认后的调整点

必备指标：

- 成功率 / 错误 / 耗时 / 成本
- Schema 或中间产物完整性
- 任务质量
- 分歧或失败数量
- 可唤起性
- 存在必要性

## 评测设计确认

> 本节说明正式执行前确认过哪些设计，避免指标和流程在执行后才大幅返工。

如果没有取得用户确认，报告不能写成正式评测结果，只能输出“评测设计草案”。用户明确确认后，才能继续执行测试并生成最终报告。

使用表格：

| 确认项 | 草案内容 | 确认结果 | 调整记录 |
| --- | --- | --- | --- |
| 三层拆解 | 运行框架层 / 能力链路层 / 业务效果层 | 已确认 / 待确认 |  |
| 评测目标 | 运行目标、能力目标、业务目标、可唤起性、必要性 | 已确认 / 待确认 |  |
| 指标体系 | 指标、计算规则、数据来源、评判方式 | 已确认 / 待确认 |  |
| 数据方案 | 来源、样本池、拆分、reference output | 已确认 / 待确认 |  |
| 测试流程 | 执行步骤、协同编排、产出路径 | 已确认 / 待确认 |  |
| 数据记录表 | 表结构、存储位置、产物路径 | 已确认 / 待确认 |  |

## 评测流程设计

> 本节说明评测如何形成闭环：从理解对象，到沉淀 eval 资产和 skill 优化方案。

包含：

- 闭环流程
- 沉淀什么、沉淀到哪里
- 失败点如何进入后续 eval
- 测试流程内的 multi-agent / sub-agent 协同编排判断：是否启用、为什么、每个角色负责什么
- 评测数据记录方案：表结构、保存目录、运行 ID 规则

## 测试方法与执行流程

> 本节说明这次评测怎么执行：数据从哪里来，如何准备，怎么拆分，什么时候用哪种评判方式。

包含：

- 必要时加入流程图
- 步骤表，列为：步骤 / 具体动作 / 对应层级 / 输出物
- 数据集拆分依据
- 评判方式选择表
- 数据记录表落盘位置

## 测试结果

> 本节只展示本轮真实测试得到的数据、现象和归因。

推荐小节：

1. `本轮执行概览`
2. `可唤起性与存在必要性检查`
3. `评测数据记录表`
4. `样本池构建与拆分结果`
5. `端到端测试`
6. `保留集验收测试`
7. `分歧样本明细`

如果某个小节没有实测数据，保留小节，但不能直接标为 `待补测` 了事。先说明已尝试或已判断的主动补测路径；只有当前工具、权限、数据或目标对象形态确实无法覆盖时，才标为 `待补测`，并说明阻塞原因和下一步应该怎么补测。

如果是文档型 / 工作流型 skill 的纯静态评测，使用这些结果小节：

1. `本轮执行概览`
2. `静态结构检查`
3. `可唤起性与存在必要性检查`
4. `发现的问题`
5. `待补测计划`，仅包含当前客观无法测量的项目

没有真实运行时，不要编造 endpoint、成本、耗时或样本质量数据。

## Skill 优化方向

> 本节把评测结果反哺回 skill。每条优化方向都应能追溯到数据或失败样本。

使用表格：

| 优化项 | 来源证据 | 建议修改 | 验证方式 |
| --- | --- | --- | --- |

如果相关，补充可唤起性和无 skill 基线的后续动作。

## 产物与复现位置

> 本节用于复现和后续接着做优化。

包含以下路径或链接：

- 原始日志
- 原始输出
- 全量数据集
- 优化集 / 保留集
- 评测设计确认记录
- 评测运行记录
- 测试样本记录
- 指标结果记录
- 评判输出
- 分歧样本
- 典型测试场景目录
- 测试流程协同记录
- 生成的报告
- 优化方案

## 出处与参考

当报告会作为后续实践参考时，应包含工作法和外部来源，例如：

1. LangChain, Evaluating Skills：https://www.langchain.com/blog/evaluating-skills
1. LangChain, Better harness: a recipe for harness hill climbing with evals：https://www.langchain.com/blog/better-harness-a-recipe-for-harness-hill-climbing-with-evals
1. Google Machine Learning Crash Course, Dividing datasets：https://developers.google.com/machine-learning/crash-course/overfitting/dividing-datasets
1. scikit-learn, `train_test_split` documentation：https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.train_test_split.html
1. LangSmith, Code evaluator：https://docs.langchain.com/langsmith/code-evaluator
